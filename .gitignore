# Byte-compiled / cache
__pycache__/
*.pyc

# Data (keep large results versioned if desired)
# Note: `/results/` is intentionally NOT ignored to allow committing artifacts

# Model weights and large artifacts
*.safetensors
*.pt
*.bin
*.model
tokenizer.model
tokenizer.json

# Unsloth compiled cache and locks
/unsloth_compiled_cache/

# OS files
.DS_Store

# Temporary files
/tmp/
*.tar.gz
